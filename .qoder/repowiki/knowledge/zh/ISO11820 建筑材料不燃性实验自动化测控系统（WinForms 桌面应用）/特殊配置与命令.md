UI 自动化测试需先构建主程序再运行：在 `tests/ISO11820.UI.Tests/` 目录下执行 `.\RunTests.ps1`（支持 `-Filter "TC01"` 筛选单个用例、`-ListTests` 列出用例）。EPPlus 非商业许可证需在启动时设置 `ExcelPackage.LicenseContext = LicenseContext.NonCommercial`（已在 Bootstrapper 中完成）。